from ..data import Train_data,Carriage_data
import numpy as np

def get_effective_data(data) ->Train_data:
    true_data = _rm_inherent_data(data)
    complete_data = _rm_loss_data(true_data)
    for i in range():
        effective_data = extract_data(complete_data)
    pass

def _rm_inherent_data(data) ->data:
    pass

def _rm_loss_data(data) ->data:
    pass

def extract_data(data) ->Carriage_data:
    pass
